<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- This new style inherits from your app's main theme -->
    <style name="Theme.SnapSpend.TransparentDialog" parent="Theme.SnapSpend">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>
    <style name="Theme.SnapSpend.AppWidgetContainer" parent="Theme.MaterialComponents.DayNight">
        <!-- You can customize this theme later if you want -->
    </style>
</resources>